#binary to integer using functions
def bin_int(num,pos=0,res=0):
    while num != 0:
        rem = num%10
        res = res + rem*2**pos
        num = num//10
        pos = pos+1
    return res
num = 1110
print(bin_int(num))
